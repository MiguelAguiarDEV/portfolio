---

---

<div
    class="section contact w-full h-full p-6 overflow-auto text-white/90 flex flex-col gap-5"
>
    <h1 class="text-2xl font-bold">Contacto</h1>
    <p class="text-sm opacity-80 leading-relaxed max-w-prose">
        Escríbeme para colaborar o revisar rendimiento de tu proyecto.
    </p>
    <form
        class="max-w-sm flex flex-col gap-3"
        onsubmit="event.preventDefault(); this.reset(); alert('Enviado (demo)');"
    >
        <input
            required
            name="name"
            placeholder="Nombre"
            class="px-3 py-2 rounded bg-white/10 border border-white/15 text-sm focus:outline-none focus:ring-2 focus:ring-white/30"
        />
        <input
            required
            type="email"
            name="email"
            placeholder="Email"
            class="px-3 py-2 rounded bg-white/10 border border-white/15 text-sm focus:outline-none focus:ring-2 focus:ring-white/30"
        />
        <textarea
            required
            name="msg"
            rows="4"
            placeholder="Mensaje"
            class="px-3 py-2 rounded bg-white/10 border border-white/15 text-sm focus:outline-none focus:ring-2 focus:ring-white/30 resize-none"
        ></textarea>
        <button
            class="mt-1 px-4 py-2 rounded bg-white/20 hover:bg-white/30 active:bg-white/40 text-sm font-medium backdrop-blur transition-colors"
            type="submit">Enviar</button
        >
    </form>
    <div class="text-xs opacity-60 mt-auto">
        Email directo: <a
            href="mailto:contacto@example.com"
            class="underline hover:opacity-100">contacto@example.com</a>
    </div>
</div>
